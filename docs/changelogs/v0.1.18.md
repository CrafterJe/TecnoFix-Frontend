### Corregido
- **Auto-updater ahora genera `latest.json` y archivos `.sig` correctamente.** Se activó `createUpdaterArtifacts: true` en `tauri.conf.json` para que el bundler de Tauri v2 firme los artefactos de actualización.
- Formato de la llave privada y pública del updater corregido a base64 (formato nativo que Tauri espera).

### Notas
- Esta versión es la primera que el auto-updater puede firmar. La actualización automática empieza a funcionar a partir de aquí.
