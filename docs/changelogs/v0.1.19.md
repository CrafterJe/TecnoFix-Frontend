### Agregado
- Plugin `@tauri-apps/plugin-process` + `tauri-plugin-process` en Rust para poder relanzar la app tras aplicar una actualización.
- Changelog por versión en `docs/changelogs/`; el workflow de release lo lee automáticamente y lo publica como descripción del release en GitHub.

### Corregido
- La app ya no se queda colgada en "Reiniciando aplicación..." después de descargar el update. Ahora el `relaunch()` del plugin `process` cierra el proceso actual y abre la versión actualizada.
