### Agregado
- Versión de la app visible en dos lugares: esquina inferior izquierda del login y al pie del sidebar (ambos modos expandido y colapsado).
- Inyección automática de `__APP_VERSION__` desde `package.json` en build time vía Vite, para no mantener la versión a mano en los displays.

### Corregido
- `vite.config.ts` dejó de reportar errores de TypeScript tras simplificar el wrapper `async () => (...)` (no había ningún `await` dentro).
